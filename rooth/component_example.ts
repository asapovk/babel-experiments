import { asComponent, tag } from "./rooth";


asComponent({
    // selector (state) => localState
    // updateEvents: ['eventName', {}] 
    // onMount (opts) =>
    // onUpdate (event, opts) => 
    // onUnmount (opts) =>
    // name: 'name',
    // noCascade calc: true
    // no Style calc: true
    // no eventListenersCalc: true
})
export const Application = (args, {state, trigger}) => {
    return tag({},
                tag({}, [
                    
                ])
            )
}